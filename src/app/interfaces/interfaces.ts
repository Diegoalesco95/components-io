export interface IComponent {
  icon: string;
  name: string;
  redirectTo: string;
}

export interface IAlbum {
  userId: number;
  id: number;
  title: string;
}

export interface IHeroes {
  superhero: string;
  publisher: string;
  alter_ego: string;
  first_appearance: string;
  characters: string;
}
